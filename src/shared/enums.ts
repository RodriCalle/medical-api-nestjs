export enum AppointmentStatus {
  ACCEPTED = 'ACCEPTED',
  CANCELED = 'CANCELED',
}

export enum DocumentType {
  CE = 'CE',
  DNI = 'DNI',
  PASSPORT = 'PASSPORT',
}

export enum Role {
  DOCTOR = 'DOCTOR',
  PATIENT = 'PATIENT',
  ADMIN = 'ADMIN',
}
